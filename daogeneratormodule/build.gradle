apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    //used for plain db
    //compile 'org.greenrobot:greendao-generator:3.1.0'

    //used for sql chipher
    compile 'org.greenrobot:greendao-generator-encryption:2.2.0'

}